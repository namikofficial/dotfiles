# Managed by dotfiles system profile
# Intel renders the desktop. Keep the unstable NVIDIA DRM frontend out of the
# boot/session path; the base nvidia module remains available for CUDA/compute.
blacklist nvidia_drm
