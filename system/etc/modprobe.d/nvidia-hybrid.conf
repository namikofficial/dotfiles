# Managed by dotfiles system profile
# Conservative policy: NVIDIA 610 open modules currently hang during runtime
# initialization on this firmware when dynamic power management is enabled.
options nvidia NVreg_DynamicPowerManagement=0x00 NVreg_PreserveVideoMemoryAllocations=1 NVreg_TemporaryFilePath=/var/tmp
