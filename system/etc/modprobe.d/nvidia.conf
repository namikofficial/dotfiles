# Managed by dotfiles system profile
# This Clevo hybrid laptop hangs Hyprland during backend initialization when
# NVIDIA DRM KMS is enabled. Intel owns the compositor and display outputs.
options nvidia_drm modeset=0 fbdev=0
