# llama-swap chat/agent router.
# Model paths are rendered by llama-swap-manager from:
#   /home/namik/llama-models/chat
# Embedding GGUFs are intentionally not configured here; run the separate
# embedding-server-manager on port 8081.

healthCheckTimeout: 120
logRequests: true

models:
  qwen3-4b-local:
    cmd: >
      __LLAMA_SERVER__
      --host 127.0.0.1
      --port ${PORT}
      --model __QWEN3_MODEL__
      --ctx-size 81920
      __GPU_LAYERS_ARG__
      --threads 8
      --parallel 1
      --jinja
    aliases:
      - qwen3-4b-local
      - qwen3-4b
      - qwen3

  granite-agent:
    cmd: >
      __LLAMA_SERVER__
      --host 127.0.0.1
      --port ${PORT}
      --model __GRANITE_MODEL__
      --ctx-size 32768
      __GPU_LAYERS_ARG__
      --threads 8
      --parallel 1
      --jinja
    aliases:
      - granite-agent
      - granite

  qwen35-4b-local:
    cmd: >
      __LLAMA_SERVER__
      --host 127.0.0.1
      --port ${PORT}
      --model __QWEN35_MODEL__
      --ctx-size 81920
      __GPU_LAYERS_ARG__
      --threads 8
      --parallel 1
      --jinja
      --reasoning on
      --reasoning-budget -1
    aliases:
      - qwen35-4b-local
      - qwen3.5-4b
      - qwen35
