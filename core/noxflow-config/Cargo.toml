[package]
name = "noxflow-config"
version = "0.1.0"
edition = "2021"
description = "NoxFlow validated configuration loading"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
thiserror = "2"
