[package]
name = "noxflow-ipc"
version = "0.1.0"
edition = "2021"
description = "Versioned NoxFlow IPC contract"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
