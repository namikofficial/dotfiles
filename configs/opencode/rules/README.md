# Lazy project rules

These rules are loaded only when a matching file is in the active task surface. The implementation may use native OpenCode glob-scoped instructions when supported; otherwise the workflow agent should read the matching rule before editing. They are intentionally short so unrelated language conventions do not consume every prompt.
