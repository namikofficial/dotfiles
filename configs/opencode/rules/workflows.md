globs: [".github/workflows/**"]

Treat workflow changes as security-sensitive. Check permissions, secret exposure, action pinning, cache scope, concurrency, and failure behavior. Do not weaken checks to make CI green.
