globs: ["**/*.controller.ts"]

Trace route callers, validation, authorization, response contracts, and integration tests before changing controller behavior. Keep transport concerns out of domain logic.
