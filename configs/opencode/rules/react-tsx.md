globs: ["**/*.tsx"]

Trace the complete user flow, loading/empty/error states, keyboard and accessibility behavior, responsive layout, and existing design-system conventions. Verify affected runtime behavior.
