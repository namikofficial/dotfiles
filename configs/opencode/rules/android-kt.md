globs: ["**/android/**/*.kt", "**/*.kt"]

Account for lifecycle, back behavior, configuration changes, insets, permissions, state restoration, coroutine cancellation, and the cheapest applicable JVM or device verification.
