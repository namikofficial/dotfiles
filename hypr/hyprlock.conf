$lock_bg = rgb(1a1b26)
$lock_fg = rgb(c0caf5)
$lock_accent = rgb(7aa2f7)
$lock_blur = 3
$lock_brightness = 0.7
$lock_vibrancy = 0.2
source = ~/.cache/hypr/theme-colors-hyprlock.conf
source = ~/.cache/hypr/lock-settings.generated.conf

background {
    monitor =
    path = ~/.cache/lock-wallpaper
    blur_passes = $lock_blur
    contrast = 0.9
    brightness = $lock_brightness
    vibrancy = $lock_vibrancy
}

input-field {
    monitor =
    size = 312, 52
    outline_thickness = 2
    dots_size = 0.22
    dots_spacing = 0.2
    dots_center = true
    outer_color = $lock_accent
    inner_color = $lock_bg
    font_color = $lock_fg
    fade_on_empty = true
    placeholder_text = Enter password...
    hide_input = false
    position = 0, -70
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%A, %d %B %H:%M')"
    color = $lock_fg
    font_size = 30
    position = 0, 92
    halign = center
    valign = center
}

label {
    monitor =
    text = Locked
    color = $lock_accent
    font_size = 13
    position = 0, 50
    halign = center
    valign = center
}
